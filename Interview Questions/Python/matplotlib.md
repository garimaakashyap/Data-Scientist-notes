# Matplotlib Interview Questions (Beginner → Advanced)

## Beginner-Level Matplotlib Questions

1. What is Matplotlib?

2. Why is Matplotlib used in Python?

3. What is the difference between Matplotlib and other plotting libraries?

4. How do you install Matplotlib?

5. How do you import Matplotlib?

6. What is the common alias for Matplotlib?

7. What is pyplot in Matplotlib?

8. What is the difference between matplotlib.pyplot and matplotlib?

9. What is a figure in Matplotlib?

10. What is an axes in Matplotlib?

11. What is the difference between figure and axes?

12. What is the plt.figure() function?

13. What is the plt.plot() function?

14. What is the plt.show() function?

15. What is the plt.savefig() function?

16. What is the difference between plt.show() and plt.savefig()?

17. How do you create a simple line plot?

18. How do you create a scatter plot?

19. What is the plt.scatter() function?

20. What is the difference between plt.plot() and plt.scatter()?

21. How do you create a bar chart?

22. What is the plt.bar() function?

23. What is the plt.barh() function?

24. What is the difference between plt.bar() and plt.barh()?

25. How do you create a histogram?

26. What is the plt.hist() function?

27. What is the difference between plt.hist() and plt.bar()?

28. How do you create a pie chart?

29. What is the plt.pie() function?

30. How do you add labels to a plot?

31. What is the plt.xlabel() function?

32. What is the plt.ylabel() function?

33. What is the plt.title() function?

34. What is the plt.legend() function?

35. How do you add a legend to a plot?

## Intermediate-Level Matplotlib Questions

36. What is the plt.subplot() function?

37. What is the plt.subplots() function?

38. What is the difference between plt.subplot() and plt.subplots()?

39. How do you create multiple subplots?

40. What is the difference between figure and subplot?

41. What is the plt.xlim() function?

42. What is the plt.ylim() function?

43. What is the plt.xscale() function?

44. What is the plt.yscale() function?

45. What is the difference between plt.xlim() and plt.xscale()?

46. What is the plt.grid() function?

47. How do you add a grid to a plot?

48. What is the plt.axhline() function?

49. What is the plt.axvline() function?

50. What is the plt.axhspan() function?

51. What is the plt.axvspan() function?

52. What is the difference between plt.axhline() and plt.axhspan()?

53. What is the plt.text() function?

54. What is the plt.annotate() function?

55. What is the difference between plt.text() and plt.annotate()?

56. What is the plt.fill() function?

57. What is the plt.fill_between() function?

58. What is the difference between plt.fill() and plt.fill_between()?

59. What is the plt.stem() function?

60. What is the plt.step() function?

61. What is the difference between plt.stem() and plt.step()?

62. What is the plt.boxplot() function?

63. What is the plt.violinplot() function?

64. What is the difference between plt.boxplot() and plt.violinplot()?

65. What is the plt.errorbar() function?

66. What is the plt.contour() function?

67. What is the plt.contourf() function?

68. What is the difference between plt.contour() and plt.contourf()?

69. What is the plt.imshow() function?

70. What is the plt.colorbar() function?

71. What is the plt.colormap() function?

72. What is the difference between plt.colormap() and plt.colorbar()?

73. What is the plt.style.use() function?

74. What are the different styles available in Matplotlib?

75. What is the difference between plt.style.use() and plt.rcParams?

76. What is the plt.rcParams dictionary?

77. How do you customize plot appearance using rcParams?

78. What is the plt.tight_layout() function?

79. What is the plt.subplots_adjust() function?

80. What is the difference between plt.tight_layout() and plt.subplots_adjust()?

81. What is the plt.figure() with figsize parameter?

82. What is the dpi parameter in plt.figure()?

83. What is the difference between figsize and dpi?

84. What is the plt.close() function?

85. What is the plt.clf() function?

86. What is the plt.cla() function?

87. What is the difference between plt.close(), plt.clf(), and plt.cla()?

88. What is the object-oriented interface in Matplotlib?

89. What is the difference between pyplot interface and object-oriented interface?

90. How do you use the object-oriented interface?

## Advanced Matplotlib Questions

71. What is the difference between fig, ax = plt.subplots() and plt.plot()?

72. What is the difference between plt.xlabel() and ax.set_xlabel()?

73. What is the ax.twinx() method?

74. What is the ax.twiny() method?

75. What is the difference between ax.twinx() and ax.twiny()?

76. What is the ax.inset_axes() method?

77. What is the ax.secondary_xaxis() method?

78. What is the ax.secondary_yaxis() method?

79. What is the difference between ax.secondary_xaxis() and ax.twinx()?

80. What is the plt.animation module?

81. What is FuncAnimation in Matplotlib?

82. How do you create animated plots?

83. What is the difference between FuncAnimation and ArtistAnimation?

84. What is the mpl_toolkits.mplot3d module?

85. How do you create 3D plots?

86. What is the ax.plot_surface() method?

87. What is the ax.scatter3D() method?

88. What is the difference between ax.plot_surface() and ax.scatter3D()?

89. What is the plt.patches module?

90. How do you add custom shapes to plots using patches?

91. What is the plt.transforms module?

92. What is the difference between data coordinates and display coordinates?

93. What is the difference between transData and transAxes?

94. What are the different backends in Matplotlib?

95. What is the difference between TkAgg and Qt5Agg backends?

96. What is the difference between plt.ioff() and plt.ion()?

97. What is event handling in Matplotlib?

98. What are colormaps in Matplotlib?

99. What is the difference between qualitative and quantitative colormaps?

100. What are the best practices for creating publication-quality plots in Matplotlib?

